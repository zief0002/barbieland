---
title: "day-03-slide-code"
format: html
editor: visual
---

```{r}
#This code chunk allows us to load the packages needed for our activity
library(tidyverse)
library(ggformula)
```

```{r}
#This code chunk allows us to read in data
lego <- read_csv("lego_sample.csv")
```

```{r}
head(lego)
```

```{r}
df_stats( ~ theme, data = lego, "counts")
```

```{r}
df_stats( ~ theme, data = lego, "props")
```

```{r}
gf_counts(~ theme, data = lego)
```

```{r}
gf_props(
  ~size, data = lego
  )
```

```{r}
df_stats(~price, data = lego)
```

```{r}
df_stats(~pieces, data = lego)
```

```{r}
gf_histogram(~ price, data = lego)
```

```{r}
gf_density(~ price, data = lego)
```
